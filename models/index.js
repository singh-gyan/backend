const catalogModel = require('../models/catalog.model');
const userModel = require('../models/user.model');
const mobileModel = require('../models/mobile.model');
const headphoneModel = require('../models/headphone.model');
const guitarModel = require('../models/guitar.model');

module.exports = {
  catalogModel,
  userModel,
  mobileModel,
  headphoneModel,
  guitarModel,
};
